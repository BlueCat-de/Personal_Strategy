# 回测系统交易逻辑文档

## 1. 文档范围

本文档描述当前回测系统的实际执行逻辑，重点覆盖：

- 策略入场信号
- 策略出场信号
- 目标仓位生成
- A 股 T+1 执行
- 100 股整数手
- 45,000 元初始资金
- 滑点、佣金、印花税、过户费
- 涨跌停、成交容量、现金约束
- 止损止盈
- 当前真实性缺口和已识别问题

核心实现文件：

- `strategies/akshare_strategy_runtime.py`
- `batch_strategy_backtest.py`
- 各策略适配脚本 `strategies/**/akshare_*.py`

当前推荐回测结果目录：

- `data/backtests/jqdata_12m_realistic_tplus1_weekly_open_restore_45k_no_single_symbol`

## 2. 整体策略概述

系统不是单一策略，而是一个统一回测运行时。每个策略适配脚本负责生成目标权重，统一运行时负责把目标权重转换为真实账户中的现金、持股数量和交易记录。

当前支持的主要策略族：

| 策略族 | 核心函数 | 主要用途 |
|---|---|---|
| 动量轮动 | `momentum_rotation` | 按多个回看窗口收益排名，持有排名靠前标的 |
| 多因子轮动 | `factor_rank_rotation` | 用本地代理因子打分，定期换仓 |
| 单票技术择时 | `technical_signal_strategy` | SMA 金叉/死叉或 RSI 均值回归，带止损止盈 |
| Kelly/SMA 切换 | `sma_kelly_strategy` | 风险资产和避险资产之间切换，并按历史交易盈亏估算 Kelly 仓位 |
| Connors RSI 空头 | `connors_rsi_short_strategy` | 原始为空头均值回归，但当前 long-only 下会被裁剪为空仓 |
| 日历季节性多空 | `seasonality_long_short` | 原始为多空季节性，当前 long-only 下只保留多头腿 |
| 配对交易 | `pair_zscore_strategy` | 原始为多空配对，当前 long-only 下只能保留多头腿 |

系统当前默认是个人 A 股 long-only 账户口径，不支持融资融券、裸卖空、日内回转，也不做分钟级或逐笔撮合。

## 3. 全局回测参数

当前默认参数来自 `realistic_backtest_settings()`：

| 参数 | 默认值 | 含义 |
|---|---:|---|
| `AKSHARE_REALISTIC_BACKTEST` | `1` | 开启真实撮合路径 |
| `AKSHARE_LONG_ONLY` | `1` | 禁止负仓位 |
| `AKSHARE_INITIAL_CASH` | `45000` | 初始资金 45,000 元 |
| `AKSHARE_MAX_GROSS_EXPOSURE` | `1.0` | 总仓位不超过 100% |
| `AKSHARE_LIMIT_THRESHOLD` | `0.095` | 涨跌停近似阈值 9.5% |
| `AKSHARE_SLIPPAGE_RATE` | `0.001` | 单边滑点 0.1% |
| `AKSHARE_MAX_PARTICIPATION` | `0.005` | 单日成交额参与率上限 0.5% |
| `AKSHARE_TRADE_DELAY_DAYS` | `1` | 信号延迟 1 个交易日执行 |
| `AKSHARE_T_PLUS_ONE` | `1` | 开启 A 股 T+1 |
| `AKSHARE_LOT_SIZE` | `100` | 最小交易单位 100 股 |
| `AKSHARE_COMMISSION_RATE` | `0.0003` | 佣金率 0.03% |
| `AKSHARE_MIN_COMMISSION` | `5` | 每笔最低佣金 5 元 |
| `AKSHARE_STAMP_TAX_RATE` | `0.001` | 卖出印花税 0.1% |
| `AKSHARE_TRANSFER_FEE_RATE` | `0.00001` | 过户费 0.001% |
| `AKSHARE_EXECUTION_PRICE_FIELD` | `open` | 默认用下一交易日开盘价近似成交 |

## 4. 股票池过滤逻辑

批量回测默认从离线数据目录读取 `universe.csv`，并执行以下过滤：

- 排除创业板：`300`、`301`
- 排除科创板：`688`、`689`
- 排除 ST/*ST：名称中包含 `ST`
- 默认剔除固定单票择时策略

注意：北交所股票并未在 `batch_strategy_backtest.py` 的 `load_universe()` 中显式过滤 `4`、`8`、`920` 前缀；当前结果目录中校验未发现实际持仓包含北交所标的，但代码层面建议补充显式过滤。

## 5. 信号生成与执行时序

当前执行时序为：

1. 策略在 T 日收盘后，根据截至 T 日的日线数据生成目标权重。
2. 运行时对目标权重执行 `shift(1)`，即 T 日信号在 T+1 日执行。
3. T+1 日使用 `open` 价格作为成交基准价。
4. 买入价 = `open * (1 + slippage_rate)`。
5. 卖出价 = `open * (1 - slippage_rate)`。
6. 当日收盘后，持仓按 `close` 估值，生成权益曲线。

这符合“日线策略、收盘信号、下一交易日开盘执行”的近似实盘口径。

## 6. 仓位计算方式

### 6.1 目标权重约束

策略先生成目标权重矩阵。进入撮合前会执行：

- 缺失权重向前填充；
- `long_only=1` 时，所有负权重被裁剪为 0；
- 若总绝对权重大于 `max_gross_exposure`，按比例缩放到上限。

因此，所有空头、多空、杠杆策略在默认配置下都会被降级为 long-only 非杠杆策略。

### 6.2 目标股数转换

撮合日用开盘价估算账户价值：

```text
portfolio_value = cash + 当前持股数量 * 当日开盘价
desired_value = 目标权重 * portfolio_value
desired_shares = desired_value / 当日开盘价
```

之后按 100 股向下取整：

```text
desired_shares = floor(desired_shares / 100) * 100
```

实际买卖请求：

```text
request_shares = desired_shares - 当前持股数量
```

### 6.3 买卖顺序

系统先卖后买：

1. 先执行所有卖出，释放现金；
2. 再执行所有买入；
3. 买入时如果现金不足或总仓位超过上限，会逐手减少买入数量，直到满足约束；
4. 如果减少到 0 股，则记录为 `lot_blocked_weight_change`。

## 7. 入场信号判断逻辑

### 7.1 动量轮动

函数：`momentum_rotation`

默认逻辑：

- 调仓频率：周频；
- 对每个标的计算多个窗口的收益率；
- 默认窗口常见为 `21`、`63`、`126` 个交易日；
- 得分为各窗口收益率的加权和；
- 按得分从高到低排序；
- 持有前 `top_n` 个标的；
- 等权分配仓位。

入场条件：

```text
某标的在调仓日动量得分进入前 top_n
```

### 7.2 多因子轮动

函数：`factor_rank_rotation`

当前多因子为本地代理因子，不是 BigQuant 私有因子的完整还原。系统计算：

- 5/20/60/120 日动量；
- 20/60 日波动率；
- 5 日均量相对 60 日均量的成交量加速；
- 下行波动率；
- 60 日回撤；
- 流动性排名。

不同 `factor_profile` 使用不同组合：

- `value_quality`
- `low_vol`
- `turnover_volume`
- `intraday`
- `growth`
- `machine_learning`
- `convertible_bond`
- `etf_allocation`
- `multi_factor`

入场条件：

```text
调仓日综合得分排名靠前，且补足当前目标持仓数量
```

### 7.3 单票技术择时

函数：`technical_signal_strategy`

支持两类模式。

SMA 趋势模式：

```text
短均线 > 长均线时入场
```

RSI 均值回归模式：

```text
RSI < 30 时入场
```

入场后目标权重为 100%。

### 7.4 Kelly/SMA 切换

函数：`sma_kelly_strategy`

入场条件：

```text
风险资产 1 日均线 > 6 日均线
```

前 40 次完整交易之前，系统默认持有避险资产。积累 40 次交易盈亏后，根据胜率和盈亏比计算 Kelly 仓位。

### 7.5 Connors RSI 空头

函数：`connors_rsi_short_strategy`

原始入场条件：

```text
CRSI > 90 且 100 日年化波动率 > 100
```

原始目标是做空最多 40 只，每只 -2.5%。但当前 `long_only=1` 会把负权重裁剪为 0，因此默认批量回测中该策略不产生实际交易。

### 7.6 日历季节性多空

函数：`seasonality_long_short`

入场条件：

- 每周调仓；
- 查看上一年同月历史表现；
- 表现最好的 5 只做多；
- 表现最差的 5 只做空。

当前 `long_only=1` 会裁剪空头腿，只保留多头腿。如果回测窗口不足以提供上一年同月历史，则策略保持空仓。

### 7.7 配对交易

函数：`pair_zscore_strategy`

入场条件：

- 计算两个标的价格比值；
- 计算 rolling z-score；
- `z > distance` 时进入“空 s1、多 s2”；
- `z < -distance` 时进入“多 s1、空 s2”。

当前 `long_only=1` 会裁剪空头腿，所以不再是市场中性配对交易。

## 8. 出场信号判断逻辑

### 8.1 动量轮动出场

出场发生在调仓日：

```text
持仓标的不再位于目标排名集合中
```

非调仓日目标权重会向前延续，不会自动清仓。

### 8.2 多因子轮动出场

出场发生在调仓日：

- 先构造 `hold_count + change_num` 的候选保留池；
- 当前持仓若不在候选池中则卖出；
- 对仍在候选池中的持仓，额外卖出排名靠后的 `change_num` 只；
- 再从高分标的中补足到 `hold_count`。

非调仓日持仓延续。

### 8.3 单票技术择时出场

SMA 趋势模式：

```text
短均线 < 长均线时出场
```

RSI 均值回归模式：

```text
RSI > 70 时出场
```

此外还会触发止损止盈，见第 10 节。

### 8.4 Kelly/SMA 出场

当风险资产 1 日均线不再高于 6 日均线时，结束风险资产交易并记录一次交易盈亏。之后切回避险资产。

### 8.5 空头/多空策略出场

Connors RSI 空头：

```text
CRSI < 30 时从 current 集合移除
```

季节性多空：

```text
下一次周频调仓时重新生成多空篮子
```

配对交易：

```text
state = -1 且 z <= 0 时退出
state = 1 且 z >= 0 时退出
```

但默认 long-only 会改变这些策略的真实含义。

## 9. 止损规则

当前只有 `technical_signal_strategy` 实现显式止损。

止损判断：

```text
当前收盘价 <= 入场记录价 * (1 - stop_loss)
```

触发后，目标权重从 1 变成 0。由于系统默认 T+1 执行，实际卖出发生在下一交易日开盘，并受到跌停禁卖、T+1、成交容量和 100 股整数手约束。

重要限制：

- 止损使用日线收盘价判断，不是盘中触发；
- 不使用最低价判断是否盘中破位；
- 不支持移动止损，虽然变量中有 `peak_price`，当前没有用于 trailing stop；
- 入场价是信号生成时的收盘价，不是实际成交开盘价，因此止损比例与真实持仓成本存在偏差。

## 10. 止盈规则

当前只有 `technical_signal_strategy` 实现显式止盈。

止盈判断：

```text
当前收盘价 >= 入场记录价 * (1 + stop_profit)
```

触发后，目标权重从 1 变成 0，下一交易日开盘尝试卖出。

重要限制：

- 止盈使用收盘价判断，不是盘中触发；
- 不使用最高价判断是否盘中达到止盈；
- 不支持移动止盈；
- 入场价不是实际成交价。

## 11. 手续费和滑点处理

### 11.1 滑点

滑点按成交方向处理：

```text
买入成交价 = 执行基准价 * (1 + 0.001)
卖出成交价 = 执行基准价 * (1 - 0.001)
```

默认执行基准价为下一交易日 `open`。

### 11.2 佣金

每笔、每个标的单独计算：

```text
commission = max(5, 成交金额 * 0.0003)
```

### 11.3 印花税

只在卖出时收取：

```text
stamp_tax = 卖出成交金额 * 0.001
```

真实性提示：A 股当前印花税实际税率需要按最新监管规则确认；代码默认值为 0.1%，可能偏保守。

### 11.4 过户费

当前对所有股票买卖都收取：

```text
transfer_fee = 成交金额 * 0.00001
```

真实性提示：真实过户费规则可能区分市场和品种，当前实现未区分沪深市场、ETF、可转债等。

## 12. 特殊情况处理逻辑

### 12.1 T+1

系统维护 `sellable_shares`：

- 当天买入后不会立刻加入可卖数量；
- 每个交易日结束后，当前持仓复制为下一交易日可卖数量；
- 因此同一交易日新买入股票不能卖出。

### 12.2 涨跌停

系统使用收盘价相对上一收盘价的涨跌幅近似判断：

```text
daily_ret >= 9.5% 视为涨停，禁止买入
daily_ret <= -9.5% 视为跌停，禁止卖出
```

限制：

- 当前执行价是开盘价，但涨跌停判断使用收盘涨跌幅；
- 可能漏掉“开盘一字涨停后开板”的真实排队问题；
- 也可能误拦截“收盘涨停但开盘可成交”的订单；
- 未区分 10%、20%、5% 等不同涨跌幅制度。

### 12.3 成交容量

若 `amount` 字段存在：

```text
单标的最大成交金额 = 当日成交额 * 0.5%
```

买卖股数会按容量上限向下取整到 100 股。

若 `amount` 缺失或为空，系统将容量设为无限，这意味着该标的不会受到成交额约束。

### 12.4 现金不足

买入时会检查：

```text
成交金额 + 手续费 <= 当前现金
```

若不满足，则每次减少 100 股，直到满足约束或变为 0。

### 12.5 100 股整数手

所有目标股数和实际成交股数都向下取整到 100 股整数倍。

已用最新结果目录校验：

- 47 个策略全部成功；
- 所有实际成交股数均为 100 股整数倍；
- 没有负现金；
- 没有负费用；
- 首日权益均为 45,000 元。

### 12.6 实际持仓输出

`akshare_target_weights.csv` 实际保存的是撮合后的实际持仓权重，而不是策略原始目标权重。

`akshare_trades.csv` 包含：

- `requested_weight_change`
- `target_weight_change`
- `blocked_weight_change`
- `tplus1_blocked_weight_change`
- `executed_shares`
- `trade_notional`
- `commission`
- `stamp_tax`
- `transfer_fee`
- `lot_blocked_weight_change`

## 13. 审查发现的问题和真实性缺口

### P1：空头和多空策略在默认 long-only 下语义改变

受影响策略：

- Connors RSI 空头策略；
- 日历季节性多空策略；
- 配对交易策略；
- 其他生成负权重的策略。

当前 `long_only=1` 会把负权重裁剪为 0。这符合个人 A 股普通账户限制，但会导致原始策略语义改变：

- 空头策略可能完全空仓；
- 配对策略不再市场中性；
- 多空策略只剩多头腿。

结论：这不是撮合 bug，但不能把这些策略的结果解释为原始多空策略表现。

### P1：止损止盈不是实盘级别触发

当前止损止盈只在单票技术策略中按收盘价判断，并在下一交易日开盘执行。它无法模拟：

- 盘中触发止损；
- 盘中触发止盈；
- 跳空导致的真实成交偏差；
- 以实际成交价为基准的止损止盈。

结论：当前适合日线收盘策略，不适合声称为严格实盘止损模型。

### P1：涨跌停判断与开盘执行价不完全一致

当前执行价为下一交易日开盘价，但涨跌停阻塞使用收盘涨跌幅近似。

风险：

- 开盘涨停但收盘未涨停的买单可能被错误允许；
- 开盘可交易但收盘涨停的买单可能被错误阻塞；
- 不区分主板、ST、创业板、科创板、北交所不同涨跌幅制度。

建议后续用 `open/high/low/close` 和前收盘价重建更细的日线级可交易状态，例如：

- `open >= prev_close * limit` 判断开盘不可买；
- `low >= prev_close * limit` 判断全天一字涨停；
- `open <= prev_close * down_limit` 判断开盘不可卖；
- `high <= prev_close * down_limit` 判断全天一字跌停。

### P2：阻塞交易归因存在混杂

`tplus1_blocked_weight_change` 当前根据最终 `request_shares - exec_share_delta` 的卖出差额计算。由于 `exec_share_delta` 已经经过涨跌停、T+1、容量等多轮修改，该字段可能混入非 T+1 原因。

`blocked_weight_change` 当前是请求权重变化减执行权重变化。请求权重基于开盘估值，执行权重记录时使用收盘估值，两者价格口径不同，因此阻塞统计只能作为近似。

结论：交易结果本身可用，但阻塞原因拆分不够精确。

### P2：100 股整数手导致的“目标买 0 股”未完全记入 lot blocked

当目标权重对应金额不足以买 100 股时，`desired_shares` 会先被取整为 0。如果当前也没有持仓，则 `request_shares=0`，不会记录为 `lot_blocked_weight_change`。

结果：

- 成交逻辑正确；
- 但“因资金太小买不起一手”的机会损失没有完整统计。

### P2：周频调仓只选择周一交易日

`rebalance_dates(..., "weekly")` 当前只选择 weekday 为 0 的交易日。如果周一休市，该周不会自动顺延到周二。

这与很多平台的 `rebalance_period='week'`、`roll_forward=True` 不完全一致。

### P2：非真实撮合路径存在额外信号延迟风险

当 `AKSHARE_REALISTIC_BACKTEST=0` 或未提供 `market_panel` 时，系统进入简化权重回测路径。该路径先对权重做 `shift(trade_delay_days)`，收益计算又使用 `constrained.shift(1) * returns`，可能造成额外一日延迟。

当前批量结果使用真实撮合路径，因此不影响最新推荐结果，但建议修正或弃用简化路径。

### P2：费用规则未区分市场和品种

当前手续费模型统一适用于所有标的：

- 股票；
- ETF；
- 可转债代理；
- 深市/沪市。

真实市场中，印花税、过户费、ETF 费用、可转债费用并不完全相同。

### P3：成交容量依赖 `amount` 字段质量

若数据源没有 `amount` 或字段为空，容量约束会退化为无限容量。当前 JQData 数据一般有 `amount`，但多源 fallback 时需要继续校验。

### P3：策略输出文件命名容易误解

`akshare_target_weights.csv` 当前保存的是实际持仓权重，不是策略原始 target weights。严格来说应拆分为：

- `akshare_desired_weights.csv`
- `akshare_actual_weights.csv`

当前命名可能让后续分析误以为里面是未经撮合约束的目标权重。

## 14. 当前实现与设计需求一致性结论

已符合的需求：

- 日线级别回测；
- T 日信号，T+1 执行；
- 默认下一交易日开盘价执行；
- 初始资金 45,000 元；
- 100 股整数手；
- 现金和持股数量真实撮合；
- long-only；
- 总仓位不超过 100%；
- 涨停禁买、跌停禁卖的保守近似；
- 0.1% 滑点；
- 成交额参与率上限；
- 佣金最低 5 元；
- 卖出印花税；
- 过户费；
- 非调仓日持仓延续；
- 默认排除创业板、科创板、ST；
- 最新 47 个策略全部成功运行。

仍不完全真实的部分：

- 止损止盈不是盘中触发；
- 涨跌停用收盘涨跌幅近似，不是开盘/全天可交易状态；
- 空头和多空策略被 long-only 改写；
- 费用未区分不同市场和品种；
- 周一休市时周频调仓不会自动顺延；
- 买不起一手的目标机会损失统计不完整；
- 阻塞原因统计存在混杂；
- 简化权重回测路径存在额外延迟风险；
- 多数 BigQuant/GitHub 策略仍为代理复现。

## 15. 建议修复优先级

### 第一优先级

1. 将涨跌停判断改为基于 `open/high/low/close` 的日线可交易状态。
2. 拆分阻塞原因：涨跌停、T+1、容量、现金、整数手分别记录。
3. 修正周频调仓，周一非交易时顺延到该周首个交易日。
4. 将 `akshare_target_weights.csv` 拆分为目标权重和实际权重。

### 第二优先级

1. 止损止盈改为以实际成交成本为基准。
2. 可选支持日线级盘中触发近似：用 `low` 判断止损，用 `high` 判断止盈，但执行价仍需保守处理。
3. 修正或移除非真实撮合路径的额外延迟问题。
4. 费用按股票、ETF、可转债、沪深市场分别配置。

### 第三优先级

1. 对多空策略单独标记为“不适合普通 A 股 long-only 账户”。
2. 对买不起一手的目标记录 `unaffordable_lot_blocked`。
3. 对 BigQuant 私有因子策略继续做原始还原审计。
